---
/**
 * Homepage — Course Landing Page
 * ───────────────────────────────
 * The main entry point of the course. Displays a hero section and
 * chapter cards linking to the first lesson of each chapter.
 *
 * This page demonstrates:
 * - Importing and using a layout component
 * - Fetching data at build time in the frontmatter
 * - Rendering a list of items from a content collection
 *
 * @see Lesson 1.2 (Project Structure) for how pages map to routes.
 * @see Lesson 3.2 (Querying Content) for getCollection usage.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import ChapterCard from '../components/ChapterCard.astro';
import { getLessonsByChapter, getLessonSlug, CHAPTERS } from '../utils/lessons';
import { url } from '../utils/paths';

const lessonsByChapter = await getLessonsByChapter();
---

<BaseLayout
  title="Astro for React Developers — Interactive Course"
  description="Learn the Astro framework through hands-on lessons. Built for experienced React developers who want to master Astro's islands architecture, content collections, and more."
>
  <div class="homepage">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <span class="hero-badge">Free Interactive Course</span>
        <h1>Astro for<br /><span class="accent">React Developers</span></h1>
        <p class="hero-description">
          You know React. Now learn Astro — the web framework that ships
          <strong>zero JavaScript by default</strong> and uses an islands architecture
          for surgical interactivity. Go from React-only to full-stack Astro in 15 lessons.
        </p>
        <div class="hero-cta">
          {lessonsByChapter.get(1)?.[0] && (
            <a href={url(`/lessons/${getLessonSlug(lessonsByChapter.get(1)![0])}/`)} class="btn btn-primary">
              Start Learning →
            </a>
          )}
          <a href={url('/lessons')} class="btn btn-secondary">
            View All Lessons
          </a>
        </div>
        <div class="hero-stats">
          <span>📚 15 Lessons</span>
          <span>⏱️ ~4 hours</span>
          <span>🏗️ 5 Chapters</span>
          <span>⚡ Hands-on</span>
        </div>
      </div>
    </section>

    <!-- Chapter Cards -->
    <section class="chapters">
      <h2>Course Chapters</h2>
      <div class="chapter-grid">
        {[...lessonsByChapter.entries()].map(([chapterNum, lessons]) => (
          <ChapterCard
            number={chapterNum}
            title={CHAPTERS[chapterNum]?.title ?? ''}
            description={CHAPTERS[chapterNum]?.description ?? ''}
            lessonCount={lessons.length}
            firstLessonSlug={getLessonSlug(lessons[0])}
          />
        ))}
      </div>
    </section>

    <!-- About Section -->
    <section class="about">
      <h2>How This Course Works</h2>
      <div class="about-grid">
        <div class="about-card">
          <h3>📖 Read the Lessons</h3>
          <p>
            Each lesson is a blog-style article with explanations, code examples,
            and interactive demos. Start from Chapter 1 and work your way through.
          </p>
        </div>
        <div class="about-card">
          <h3>🔍 Explore the Source</h3>
          <p>
            This course is built with Astro! After the first few lessons, you'll be able to
            read and understand the project's own source code as a learning resource.
          </p>
        </div>
        <div class="about-card">
          <h3>🧪 Try the Demos</h3>
          <p>
            Interactive React components are embedded throughout the lessons to demonstrate
            Astro's islands architecture in action.
          </p>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  .homepage {
    max-width: 960px;
    margin: 0 auto;
    padding: 0 var(--space-6);
  }

  /* Hero */
  .hero {
    padding: var(--space-16) 0 var(--space-12);
    text-align: center;
  }

  .hero-badge {
    display: inline-block;
    padding: var(--space-1) var(--space-4);
    background: var(--color-accent-subtle);
    color: var(--color-accent-light);
    border-radius: 999px;
    font-size: var(--font-size-sm);
    font-weight: 600;
    margin-bottom: var(--space-6);
  }

  .hero h1 {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: var(--space-6);
  }

  .accent {
    color: var(--color-accent-light);
  }

  .hero-description {
    font-size: var(--font-size-lg);
    color: var(--color-text-secondary);
    max-width: 600px;
    margin: 0 auto var(--space-8);
    line-height: 1.7;
  }

  .hero-cta {
    display: flex;
    gap: var(--space-4);
    justify-content: center;
    margin-bottom: var(--space-8);
  }

  .btn {
    display: inline-block;
    padding: var(--space-3) var(--space-6);
    border-radius: var(--border-radius);
    font-weight: 600;
    font-size: var(--font-size-base);
    text-decoration: none;
    transition: all var(--transition-fast);
  }

  .btn-primary {
    background: var(--color-accent);
    color: white;
  }

  .btn-primary:hover {
    background: var(--color-accent-light);
    color: white;
    text-decoration: none;
  }

  .btn-secondary {
    border: 1px solid var(--color-border);
    color: var(--color-text-secondary);
  }

  .btn-secondary:hover {
    border-color: var(--color-text-muted);
    color: var(--color-text);
    text-decoration: none;
  }

  .hero-stats {
    display: flex;
    gap: var(--space-6);
    justify-content: center;
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  /* Chapters */
  .chapters {
    padding: var(--space-12) 0;
  }

  .chapters h2 {
    text-align: center;
    margin-bottom: var(--space-8);
  }

  .chapter-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-4);
  }

  /* About */
  .about {
    padding: var(--space-12) 0 var(--space-16);
  }

  .about h2 {
    text-align: center;
    margin-bottom: var(--space-8);
  }

  .about-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--space-4);
  }

  .about-card {
    padding: var(--space-6);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    background: var(--color-bg-secondary);
  }

  .about-card h3 {
    margin-top: 0;
    margin-bottom: var(--space-2);
    font-size: var(--font-size-lg);
  }

  .about-card p {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2.5rem;
    }

    .hero-stats {
      flex-wrap: wrap;
      gap: var(--space-3);
    }

    .hero-cta {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
